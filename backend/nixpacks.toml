[phases.setup]
cmds = ["npm install"]

[start]
cmd = "npm start"

[build]
builder = "nixpacks"
buildCommand = "npm install"
startCommand = "npm start"

[variables]
NODE_ENV = "production"
PORT = "8080"