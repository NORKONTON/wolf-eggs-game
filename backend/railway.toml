[build]
builder = "nixpacks"
buildCommand = "./build.sh"
startCommand = "./start.sh"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"

[env]
NODE_ENV = "production"
PORT = "8080"